# Requirements for Linux with NVIDIA GPU (ASUS ROG with RTX 2060)
# Install: pip install -r requirements-linux.txt

# faster-whisper for CUDA-accelerated transcription
faster-whisper>=1.0.0

# PyTorch with CUDA support (for GPU detection)
# Note: Install separately with: pip install torch --index-url https://download.pytorch.org/whl/cu118
torch

# Supabase for persistence (optional)
supabase>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Progress bars
tqdm>=4.65.0

# OpenAI API (optional, for cloud transcription fallback)
openai>=1.0.0

# Audio file handling
pydub>=0.25.0

# Local server
flask>=3.0.0
flask-cors>=4.0.0
